# 0.3.5 Drift and Verification Conflicts Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add deterministic runtime, package-manager, and verification-command drift checks without taking on rule-quality or safety-hygiene scope.

**Architecture:** Build on existing extracted facts from package metadata, runtime files, CI commands, README commands, and agent instruction docs. Add pure checks only; do not add network calls, LLM calls, write mode, or provider runtime introspection.

**Tech Stack:** TypeScript, Vitest, existing scanner pipeline, existing text/JSON/SARIF reporters.

---

## Source spec

Use the umbrella design spec as the source of truth:

```text
docs/superpowers/specs/2026-05-15-pre-0.4-context-quality-design.md
```

This plan intentionally covers only the 0.3.5 slice.

---

## In scope

- Runtime version drift checks:
  - `.nvmrc`
  - `.node-version`
  - `package.json` `engines.node`
  - GitHub `actions/setup-node` `node-version`
  - README / agent-visible command references when deterministic
- Package manager drift checks:
  - `package.json` `packageManager`
  - lockfile type
  - setup actions
  - docs and agent-visible commands
- Verification command conflict checks across:
  - package scripts
  - CI commands
  - README commands
  - agent instruction commands

---

## Out of scope

- Rule-quality checks. Planned for 0.3.6.
- Safety/workflow hygiene visibility checks. Planned for 0.3.6.
- Synthetic dogfood corpus and launch examples. Planned for 0.3.7.
- Context health score or badge. Planned for 0.3.8 gate or 0.4+.
- Semantic LLM comparison of instructions.
- Non-deterministic style critique.
- Network lookup of tool docs.
- Automatic fixes.
- MCP context gate.
- Runtime agent probing.

---

## Candidate finding IDs

- `node-runtime-drift`
- `package-manager-drift`
- `verification-command-conflict`

Severity policy:

- `error`: direct contradiction likely to break setup or verification.
- `warning`: strong mismatch that may be intentional but should be visible.
- `info`: weak visibility guidance where evidence is useful but not decisive.

False-positive policy:

- Prefer no finding when evidence is ambiguous.
- Do not infer tool equivalence from prose.
- Treat `corepack pnpm` and `pnpm` as the same package-manager intent only after the normalization rule is tested.

---

## Candidate tasks

### Task 1: Drift design decisions and fixtures

- [ ] Define exact Node version normalization rules for `>=20`, `20.x`, exact versions, and unsupported dynamic values like `lts/*`.
- [ ] Define package-manager command normalization for `pnpm`, `corepack pnpm`, `npm`, `yarn`, and `bun`.
- [ ] Define verification-command conflict evidence rules.
- [ ] Add clean, warning, and no-finding fixtures.
- [ ] Commit the design decisions before implementation.

### Task 2: Runtime version drift extraction gaps

- [ ] Write failing tests for `.nvmrc`, `.node-version`, `package.json engines.node`, and setup-node combinations that current facts cannot express.
- [ ] Add or extend deterministic extractors only if existing facts are insufficient.
- [ ] Preserve source spans for every version fact.
- [ ] Verify targeted tests pass.
- [ ] Commit.

### Task 3: Runtime version drift check

- [ ] Write failing tests for direct Node major-version conflict.
- [ ] Write no-finding tests for aligned versions and dynamic unsupported values.
- [ ] Implement pure `node-runtime-drift` check.
- [ ] Register check without changing unrelated findings.
- [ ] Verify targeted tests pass.
- [ ] Commit.

### Task 4: Package-manager drift check

- [ ] Write failing tests for `packageManager` versus lockfile/setup-action/docs-command conflicts.
- [ ] Write no-finding tests for `corepack pnpm` and `pnpm` equivalence.
- [ ] Preserve existing `package-manager-mismatch` behavior or deliberately fold it into the new check without duplicate findings.
- [ ] Implement pure `package-manager-drift` check.
- [ ] Verify targeted tests pass.
- [ ] Commit.

### Task 5: Verification-command conflict check

- [ ] Write failing tests where agent docs tell the agent to run a different deterministic verification command than package scripts and CI.
- [ ] Write no-finding tests for weak README-only evidence and shell plumbing.
- [ ] Reuse existing command classifiers where possible.
- [ ] Implement pure `verification-command-conflict` check.
- [ ] Verify targeted tests pass.
- [ ] Commit.

### Task 6: Docs, review, and release prep

- [ ] Update README support matrix and examples.
- [ ] Update CHANGELOG, roadmap, architecture docs, and release docs.
- [ ] Run full verification gate from the umbrella spec.
- [ ] Run independent review.
- [ ] Release only if Dr. Context self-scan remains clean or accepted findings are documented.

---

## Guardrails

- Prefer false negatives over noisy false positives.
- Every finding needs source-backed evidence and a concrete suggestion.
- Do not print secrets or private paths.
- Do not add new default findings to Dr. Context self-scan unless explicitly accepted and documented.
- Keep checks pure; extractors own parsing and source mapping.
